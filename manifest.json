{
  "manifest_version": 2,

  "name": "ChromeTabKeeper",
  "description": "Extension that keeps currently open tabs saved and allows reopening of tabs that are open all at once",
  "version": "1.0",

  "browser_action": {
    "default_icon": "icon.png"
  },
  "background": {
    "scripts": ["background.js"]
  },
  "permissions": [
    "activeTab",
    "https://ajax.googleapis.com/"
  ]
}